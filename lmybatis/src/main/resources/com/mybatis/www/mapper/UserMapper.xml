<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.www.dao.UserMapper">
    <resultMap id="userResultMap" type="com.mybatis.www.domain.User">
        <id property="id" column="id" />
        <result property="name" column="name"/>
        <result property="password" column="password" />
    </resultMap>

    <sql id="userColumns">
        id, name, password
    </sql>
    <insert id="insert" parameterType="com.mybatis.www.domain.User" useGeneratedKeys="true" keyProperty="id">
        insert into user (name, password) value (#{name}, #{password})
    </insert>

    <update id="update" parameterType="com.mybatis.www.domain.User">
        update user set name = #{user.name}, password = #{user.password} where id = #{id}
    </update>

    <delete id="delete" parameterType="integer">
        delete from user where id = #{id}
    </delete>

    <delete id="batchDelete" parameterType="list">
        delete from user where id in
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <select id="getById" resultMap="userResultMap">
        select
        <include refid="userColumns" />
        from user where id = #{id}
    </select>
</mapper>